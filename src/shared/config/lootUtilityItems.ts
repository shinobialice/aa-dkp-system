// Служебные строки item_type — не обычные предметы лута, а завязанные по
// имени спецкейсы в других частях приложения (см. itemTypeAdmin.ts,
// AddLootDialog, MiscLootSummary): "В казну" — приход денег напрямую,
// "Всякие мелочи"/"Всякие мелочи 2" — помесячные сводки без поштучного учёта.
//
// Вынесено в отдельный файл (а не оставлено внутри itemTypeAdmin.ts), потому
// что тот — "use server" файл, а такие файлы могут экспортировать только
// async-функции — константу-массив они экспортировать не могут.
export const UTILITY_ITEM_NAMES = ["В казну", "Всякие мелочи", "Всякие мелочи 2"];
